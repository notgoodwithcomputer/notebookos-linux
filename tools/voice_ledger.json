{
 "_format": "allow: {exact string: reason it is FUNCTION}. pending: {file: [exact strings that ARE the voice, awaiting rewrite by the owning app lane]}. Both shelves ratchet — voice_check fails on any finding not listed here and on any pending entry whose string has left the source. Reviewed by the campaign session, task 026, 2026-08-07.",
 "allow": {
  "%s - Show Menu does not say which variable the answer goes in, so the choice would be lost.": "build diagnostic: same genre — defect plus the consequence that makes it matter",
  "%s - Show Menu has no lines in it, so there is nothing to choose from.": "build diagnostic: states the defect and its consequence; length is the genre of a diagnostic, not voice",
  "A USB drive is plugged in but cannot be identified, so it is not offered.": "empty-state fact: with a stick in the port, “No USB drive is plugged in.” is false and the person blames the stick, the port or the machine — the sentence states what was seen and why the list is empty; length is the genre of a diagnostic, not voice",
  "Add %d to Media": "imperative verb label naming the action (Add N clips to Media); the coaxing-prompt rule matched the shape, not the voice",
  "For a Game Boy Advance console, copy the file to a USB stick, then to a flashcart.": "export handoff: the dialog's function IS the procedure for using the emitted file",
  "Start the machine from the %s installer and open this app again from that desktop.": "states the required procedure; an installer that cannot run must say what to do instead",
  "The books were kept, and nothing changed here will be saved over them.": "damaged-store notice: this sentence IS the function. It tells somebody their data was kept and will not be overwritten, at the one moment they would otherwise see an empty app and assume the worst. 'Your' is the point (it is THEIR library/playlists/writing/history), and a terse form would drop the reassurance that stops them from re-entering everything. Landed 2026-08-14 with the damaged-store doctrine: read-only when the store is a recovery cache, keep saving when the store IS the data, notice either way.",
  "The password file on this computer could not be read, so nothing typed here can be accepted.": "lock-screen diagnostic: the machine has already decided it cannot check a password, so the field is dead — the sentence states that and why, where the old text (“That file could not be read.”) named a file nobody can see and still asked for a password; length is the genre of a diagnostic, not voice",
  "This copy of %s is missing the tools that prepare a disk, so it cannot install itself.": "states fact and consequence — the failure-message pattern the OS standardises on",
  "Your library could not be read": "damaged-store notice: this sentence IS the function. It tells somebody their data was kept and will not be overwritten, at the one moment they would otherwise see an empty app and assume the worst. 'Your' is the point (it is THEIR library/playlists/writing/history), and a terse form would drop the reassurance that stops them from re-entering everything. Landed 2026-08-14 with the damaged-store doctrine: read-only when the store is a recovery cache, keep saving when the store IS the data, notice either way.",
  "Your playlists could not be read": "damaged-store notice: this sentence IS the function. It tells somebody their data was kept and will not be overwritten, at the one moment they would otherwise see an empty app and assume the worst. 'Your' is the point (it is THEIR library/playlists/writing/history), and a terse form would drop the reassurance that stops them from re-entering everything. Landed 2026-08-14 with the damaged-store doctrine: read-only when the store is a recovery cache, keep saving when the store IS the data, notice either way.",
  "Your workout history could not be read. The records were kept.": "damaged-store notice: this sentence IS the function. It tells somebody their data was kept and will not be overwritten, at the one moment they would otherwise see an empty app and assume the worst. 'Your' is the point (it is THEIR library/playlists/writing/history), and a terse form would drop the reassurance that stops them from re-entering everything. Landed 2026-08-14 with the damaged-store doctrine: read-only when the store is a recovery cache, keep saving when the store IS the data, notice either way.",
  "Your writing was kept, and nothing typed here will be saved over it.": "damaged-store notice: this sentence IS the function. It tells somebody their data was kept and will not be overwritten, at the one moment they would otherwise see an empty app and assume the worst. 'Your' is the point (it is THEIR library/playlists/writing/history), and a terse form would drop the reassurance that stops them from re-entering everything. Landed 2026-08-14 with the damaged-store doctrine: read-only when the store is a recovery cache, keep saving when the store IS the data, notice either way."
 },
 "pending": {
  "gbabuild.py": [
   "this is not something you can assign to"
  ]
 }
}
